---
- name: P1
  hosts: all
  gather_facts: false
  tasks:
    - name: T1
      cisco.ios.ios_command:
        commands:
          - show run

    - name: T2
      ansible.builtin.debug:
        var: out
